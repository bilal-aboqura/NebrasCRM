import { redirect } from "next/navigation"; import ExportButton from "@/components/reports/ExportButton"; import FilterBar from "@/components/reports/FilterBar"; import { ReportHeader, defaultReportFilter } from "@/components/reports/ReportPrimitives"; import TeamComparisonTable from "@/components/reports/TeamComparisonTable"; import { getTeamComparisonReport } from "@/lib/actions/reports-actions"; import { getAuthContext, isManagementRole } from "@/lib/auth/context";
export default async function TeamPage({searchParams}:{searchParams?:Record<string,string|undefined>}){const auth=await getAuthContext();if(!isManagementRole(auth.role))redirect("/reports");const filter=defaultReportFilter(searchParams),includeInactive=searchParams?.includeInactive==="1",data=await getTeamComparisonReport(filter,includeInactive);const rows=data.reps.map(r=>({"المندوب":r.repName,"المنشآت":r.facilitiesAssigned,"المتابعات":r.followupsCompleted,"الاتصالات":r.callsLogged,"العروض":r.offersSent,"العقود":r.contractsWon,"الإيراد":r.totalRevenue}));return <section className="space-y-5"><ReportHeader title="تقرير مقارنة الفريق" description="مقارنة قابلة للفرز بين مندوبي المبيعات" action={<ExportButton fileName="تقرير-مقارنة-الفريق" rows={rows} filters={[`الفترة: ${filter.startDate} - ${filter.endDate}`,includeInactive?"يشمل غير النشطين":"النشطون فقط"]}/>}/><FilterBar startDate={filter.startDate} endDate={filter.endDate}><label className="flex items-center gap-2 pb-2"><input type="checkbox" name="includeInactive" value="1" defaultChecked={includeInactive}/>عرض المندوبين غير النشطين</label></FilterBar><TeamComparisonTable rows={data.reps}/></section>}
